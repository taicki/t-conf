[user]
	name = Taek Joo Kim
	email = taicki@gmail.com
[color]
	ui = auto
[core]
	autocrlf = false
	whitespace = cr-at-eol,-indent-with-non-tab,trailing-space
	safecrlf = true
[github]
	user = taicki
[alias]
        ch = checkout
        st = status
        co = commit
        di = diff
        dic = diff --cached
        lo = log
        br = branch
        fe = fetch
        sh = stash
        ad = add
        me = merge
        graph = log --branches --pretty=short --decorate --graph
        sgraph = log --graph --pretty=format:'%Cred%h%Creset %C(yellow)%d%Creset - %s' --abbrev-commit --date=relative
        old = "!sh -c 'git update-ref refs/old/$1 refs/heads/$1 && git branch -D $1' -"
